spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:reports;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password: P@ssw0rd
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
